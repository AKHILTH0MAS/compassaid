import 'package:flutter/material.dart';

class AcceptedHome extends StatefulWidget {
  const AcceptedHome({super.key});

  @override
  State<AcceptedHome> createState() => _AcceptedHomeState();
}

class _AcceptedHomeState extends State<AcceptedHome> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Column(),
    );
  }
}
